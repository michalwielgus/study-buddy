import Label from './Label';

const Story = {
  title: 'Components/Atoms/Label',
  component: Label,
};

export default Story;

const Template = (args) => <Label>Testowa labelka</Label>;

export const Default = Template.bind({});
